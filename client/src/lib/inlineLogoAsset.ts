import { logoImage as embeddedLogoImage } from "@/lib/inlineAssets";

// Fallback embutido: não depende de conta, branch ou URL externa para aparecer no GitHub Pages.
export const logoImage = embeddedLogoImage;
