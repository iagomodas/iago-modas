# Status do envio ao GitHub

Em 17/08/2026, a conta `overziedmodas` foi conectada no GitHub e o repositório público `overziedmodas/overzied-modas` foi confirmado como vazio e correto para receber a loja. A página de upload está aberta, com o campo de arquivos e o botão de commit disponíveis.

O projeto local validado está em `/home/ubuntu/overzied-modas`. A versão deve incluir a estrutura de aplicação, `client`, `server`, `shared`, `supabase`, `docs` e `.github`, sem enviar senhas, chaves secretas ou o arquivo ZIP dentro do repositório.

A publicação no GitHub só deve ser considerada concluída depois de conferir o commit e o workflow de Pages na aba Actions.

## Observação

O upload pela interface web precisa preservar a estrutura de pastas. Caso o campo não aceite diretórios completos, deve-se usar um método autenticado que mantenha os caminhos relativos, evitando enviar todos os arquivos soltos na raiz ou colocar apenas o ZIP.
